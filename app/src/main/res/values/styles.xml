<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="logoText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#6d6d6d</item>
        <item name="android:background">@drawable/xjtlu_logo</item>
    </style>

    <style name="myThem" parent="android:Theme">
        <item name="android:background">@drawable/xjtlu_shape</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



    <!--Toolbar主题配置-->
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textSize">16sp</item> <!-- menu字号 -->
        <item name="android:textStyle">bold</item><!-- menu粗体 -->
        <item name="actionMenuTextColor">@android:color/white</item> <!-- menu字体颜色,不要加android前缀，否则4.4以前无效 -->
        <item name="android:textColorPrimary">@android:color/white</item> <!-- 改变menu图标及隐藏菜单字体颜色 -->
    </style>







</resources>
